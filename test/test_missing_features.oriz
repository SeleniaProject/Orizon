func main() {
    // 1. ブール値リテラルテスト
    let is_true = true;
    let is_false = false;
    
    // 2. 論理演算子テスト  
    let and_result = true && false;
    let or_result = true || false;
    
    // 3. 制御フロー構造テスト
    if (is_true) {
        print("if文動作");
    }
    
    while (is_false) {
        print("while文動作"); 
        break;
    }
    
    print("未実装機能テスト");
}
