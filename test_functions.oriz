func add(a, b) {
    return a + b;
}

func multiply(x, y) {
    let result = x * y;
    return result;
}

func main() {
    let sum = add(5, 3);
    let product = multiply(4, 6);
    
    print("関数テスト完了");
}
